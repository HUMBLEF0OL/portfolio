# src/lib — Utilities

Framework-agnostic helpers.

- `utils.ts` — `cn()`, the Tailwind class-merge helper (`clsx` + `tailwind-merge`).
  Use it for all conditional class merging.
- `utils.test.ts` — its unit test (Vitest).

Add small, reusable helpers here. No services, stores, or HTTP clients exist.
