# src/styles — Extra CSS

Supplementary stylesheets imported by `src/app/globals.css`.

- `glitch.css` — glitch-effect styles for the cyberpunk theme.

The core design system (tokens, angular-clip utilities) lives in
`src/app/globals.css` — see the `design-tokens` skill.
