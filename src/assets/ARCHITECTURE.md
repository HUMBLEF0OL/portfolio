# src/assets — Imported image assets

Image files imported directly into components (e.g. `passport.jpg`,
`grid-bg.jpg`) via the `@/assets/...` alias and rendered with `next/image`.

Static assets served by URL instead live in `public/`. Keep imported,
component-coupled images here.
